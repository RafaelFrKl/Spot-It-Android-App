<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="cmpt276.assignment.myapplication">

    <permission
        android:name="cmpt276.assignment.myapplication.PRIVATE"
        android:protectionLevel="signature" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="cmpt276.assignment.myapplication.PRIVATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.MANAGE_DOCUMENTS"
        tools:ignore="ProtectedPermissions" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/a10"
        android:label="@string/app_name"
        android:roundIcon="@drawable/a10"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SavedImagesActivity"
            android:label="@string/title_activity_saved_images"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity android:name=".UsernameScreen" />
        <activity android:name=".PhotoGalleryActivity" />
        <activity android:name=".HighScores" />
        <activity android:name=".GameScreen" />
        <activity android:name=".MainMenu" />
        <activity android:name=".OptionsScreen" />
        <activity android:name=".HelpScreen" />
        <activity android:name=".MainActivity">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>